# FastFood TG – WebApp + Backend (AIogram + FastAPI)

## Run
```bash
cp .env.example .env      # BOT_TOKEN ni yozing
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python bot.py
